name: Deploy Arc

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm install --legacy-peer-deps

      - name: Deploy Contract
        env:
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          RPC_URL: ${{ secrets.RPC_URL }}
        # এখানে node--experimental-default-type=module দিয়ে রান করা হচ্ছে যেন কনফ্লিক্ট না হয়
        run: npx hardhat run scripts/deploy.js --network mainnet --experimental-default-type=module
