name: Deploy Arc

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install All Dependencies
        run: |
          npm install --legacy-peer-deps
          npm install --save-dev @nomicfoundation/hardhat-toolbox dotenv --legacy-peer-deps
          npm install ethers --legacy-peer-deps

      - name: Compile Smart Contract
        # ডেপ্লয় করার আগে এই কমান্ডটি artifacts ফোল্ডার তৈরি করবে যা মিসিং ছিল
        run: npx hardhat compile

      - name: Deploy Contract Absolute Fix
        env:
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          ARC_RPC_URL: ${{ secrets.RPC_URL }}
        run: |
          cat << 'EOF' > standalone-deploy.js
          import { ethers } from "ethers";
          import fs from "fs";
          import path from "path";

          async function main() {
            const provider = new ethers.JsonRpcProvider(process.env.ARC_RPC_URL);
            
            let rawKey = process.env.PRIVATE_KEY ? process.env.PRIVATE_KEY.trim() : "";
            if (rawKey && !rawKey.startsWith("0x") && rawKey.length === 64) {
              rawKey = "0x" + rawKey;
            }

            const wallet = new ethers.Wallet(rawKey, provider);
            console.log("Deploying contract with account:", wallet.address);
            
            // কম্পাইল হওয়া ফোল্ডার থেকে ফাইল খোঁজা হচ্ছে
            const artifactsDir = path.join(process.cwd(), "artifacts/contracts");
            if (!fs.existsSync(artifactsDir)) {
              throw new Error("Artifacts directory not found. Please compile the contract first.");
            }
            
            // সাবফোল্ডারসহ আসল .json ফাইলটি খুঁজে বের করার অটোমেটিক লজিক
            function getJsonFiles(dir) {
              let results = [];
              const list = fs.readdirSync(dir);
              list.forEach((file) => {
                file = path.join(dir, file);
                const stat = fs.statSync(file);
                if (stat && stat.isDirectory()) {
                  results = results.concat(getJsonFiles(file));
                } else if (file.endsWith(".json") && !file.endsWith(".dbg.json")) {
                  results.push(file);
                }
              });
              return results;
            }

            const jsonFiles = getJsonFiles(artifactsDir);
            if (jsonFiles.length === 0) {
              throw new Error("Compiled contract JSON artifact not found.");
            }
            
            // প্রথম ভ্যালিড কনট্রাক্ট ফাইলটি রিড করা হবে
            const contractData = JSON.parse(fs.readFileSync(jsonFiles[0], "utf8"));
            const abi = contractData.abi;
            const bytecode = contractData.bytecode;
            
            const factory = new ethers.ContractFactory(abi, bytecode, wallet);
            const contract = await factory.deploy();
            
            console.log("Waiting for deployment confirmation...");
            await contract.waitForDeployment();
            
            console.log("====================================");
            console.log("🎉 Contract Deployed Successfully!");
            console.log("Contract Address:", await contract.getAddress());
            console.log("====================================");
          }

          main().catch((error) => {
            console.error("Deployment failed:", error);
            process.exitCode = 1;
          });
          EOF
          
          NODE_OPTIONS="--experimental-default-type=module" node standalone-deploy.js
